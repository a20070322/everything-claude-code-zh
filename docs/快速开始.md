# 快速开始 - 5 分钟上手 Everything Claude Code

**目标：** 在 5 分钟内让 Everything Claude Code 在你的项目中运行起来

---

## 前置要求

在开始之前，请确保你已经：

- ✅ 安装了 [Claude Code](https://code.claude.com/docs/en/installation)
- ✅ 熟悉基本的命令行操作
- ✅ 有一个正在开发的项目

---

## 第一步：安装（1 分钟）

### 方法 1：作为插件安装（推荐）

```bash
# 1. 进入项目目录
cd your-project

# 2. 克隆中文版仓库
git clone https://github.com/your-username/everything-claude-code-zh.git ~/.claude/plugins/everything-claude-code-zh

# 3. 启动 Claude Code
claude

# 4. 在 Claude Code 中运行
/plugins

# 5. 找到 "everything-claude-code-zh" 并安装
```

### 方法 2：手动复制配置

```bash
# 1. 复制配置文件到你的项目
cp -r /path/to/everything-claude-code-zh/.claude* your-project/

# 2. 启动 Claude Code
cd your-project
claude
```

---

## 第二步：验证安装（1 分钟）

检查配置是否正确加载：

```bash
# 在 Claude Code 中运行
/agents

# 你应该看到这些代理：
- planner
- architect
- code-reviewer
- security-reviewer
- build-error-resolver
- refactor-cleaner
- tdd-guide
- e2e-runner
- 等...
```

测试一个命令：

```bash
# 运行学习命令
/learn

# Claude 会分析当前会话并提取可重用模式
```

---

## 第三步：使用核心功能（3 分钟）

### 1. 规划新功能

```
我需要实现一个用户认证功能，包含注册、登录、密码重置

使用 /plan 命令帮我规划这个功能
```

**预期结果：**
- Claude 会调用 `planner` 子代理
- 创建详细的实施计划
- 分解为可执行的步骤

### 2. 代码审查

```
审查 src/auth/UserController.ts 文件的代码质量

使用 /code-review 命令
```

**预期结果：**
- Claude 会调用 `code-reviewer` 子代理
- 检查代码质量问题
- 提供改进建议

### 3. 清理代码

```
帮我清理项目中的死代码和未使用的文件

使用 /refactor-clean 命令
```

**预期结果：**
- Claude 会调用 `refactor-cleaner` 子代理
- 识别未使用的代码
- 建议可以安全删除的文件

### 4. 学习新技能

```
我刚解决了一个 TypeScript 类型推断的问题

使用 /learn 命令让 Claude 学习这个解决方案
```

**预期结果：**
- Claude 分析会话
- 提取问题-解决方案模式
- 保存为新技能文件

---

## 第四步：定制配置（可选）

### 根据项目调整技能

如果你的项目不使用某些技术，可以删除相关技能：

```bash
# 例如：不使用 Go 语言
rm ~/.claude/skills/golang-patterns.md
rm ~/.claude/skills/golang-testing.md
rm ~/.claude/agents/go-reviewer.md
rm ~/.claude/agents/go-build-resolver.md

# 或者禁用 MCP 服务
claude plugin disable clickhouse-mcp
```

### 添加项目特定的规则

创建 `your-project/.claude/rules/project-specific.md`：

```markdown
# 项目特定规则

## 命名规范
- 组件使用 PascalCase: UserProfile.ts
- 工具函数使用 camelCase: formatDate.ts
- 常量使用 UPPER_SNAKE_CASE: API_BASE_URL

## 文件组织
- 每个功能一个文件夹
- 组件和测试在同一目录
- 共享类型放在 types/ 目录

## Git 工作流
- 使用 Conventional Commits
- 每个 PR 必须通过代码审查
- 主分支受保护，需要 PR 合并
```

---

## 常见使用场景

### 场景 1：开始新功能开发

```
我需要开发一个商品管理功能

使用 /plan 规划，然后开始实现
```

### 场景 2：修复 Bug

```
有个 TypeError: Cannot read property 'xxx' of undefined

使用 /verify 验证修复，然后 /code-review 审查代码
```

### 场景 3：代码重构

```
这个组件太大了（500 行），需要重构

使用 /refactor-clean 清理和重构
```

### 场景 4：编写测试

```
为这个服务编写单元测试

使用 /tdd 进入 TDD 模式
```

### 场景 5：安全检查

```
检查这段代码是否有安全漏洞

使用 /security-review 进行安全审查
```

---

## 故障排查

### 问题 1：命令不起作用

**症状：** 运行 `/plan` 没有反应

**解决方案：**
```bash
# 检查命令文件是否存在
ls ~/.claude/commands/plan.md

# 检查文件格式是否正确
head -20 ~/.claude/commands/plan.md

# 重启 Claude Code
claude
```

### 问题 2：子代理不工作

**症状：** 提示 "Agent not found"

**解决方案：**
```bash
# 检查代理配置
cat ~/.claude/settings.json | grep agents

# 确认插件已安装
claude plugin list

# 重新安装插件
claude plugin install everything-claude-code-zh
```

### 问题 3：技能没有被激活

**症状：** Claude 没有使用项目技能

**解决方案：**
```bash
# 检查技能文件
ls ~/.claude/skills/

# 手动激活技能
"请使用 coding-standards 技能"

# 检查技能描述是否清晰
cat ~/.claude/skills/coding-standards/SKILL.md
```

---

## 下一步

现在你已经完成了快速开始，可以：

1. **深入学习：**
   - 阅读 [长篇指南](./translated/the-longform-guide.md)
   - 查看 [组件详解](./组件详解.md)

2. **探索更多功能：**
   - 尝试不同的命令
   - 测试各种子代理
   - 配置自定义 Hooks

3. **定制配置：**
   - 添加项目特定规则
   - 创建自己的技能
   - 调整子代理配置

4. **查看实战案例：**
   - 阅读 [实战案例](./实战案例.md)
   - 学习最佳实践

---

## 需要帮助？

- 📖 查看 [完整文档](../README.md)
- 💡 查看 [术语对照表](./术语对照表.md)
- 🔍 查看 [常见问题](./常见问题.md)
- 🐛 [报告问题](https://github.com/your-username/everything-claude-code-zh/issues)

---

**预计完成时间：** 5 分钟
**难度级别：** ⭐ (初级)
**下一步：** [组件详解](./组件详解.md)

---

*最后更新：2025-01-30*
